v0.8
Added Romanian names (thanks hmlendea).
Evans update.

v0.7
Changed DefaultNationNames to default to true.

v0.6
Added mod option to skip adding names list to existing nations.

v0.5
Fixed name issue.
Added a bunch of Finnish names thanks to Jopo80.

v0.4
Converted all flags to png for some space saving.
Replaced built-in nation flags with the larger ones from this mod (also removed dupes).
Slightly faster loading.

v0.3
List of all names is now applied to built-in countries.
Random birthplace defaults to true (use Mod Option to disable).
Now with 10% more randomness (or something like that).
Also now packed in an .hpk for size (5MB -> under 1MB).

v0.2
Da Vinci
