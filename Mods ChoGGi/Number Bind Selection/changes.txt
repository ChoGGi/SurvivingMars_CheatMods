v0.5
One of the funcs I override was changed (not sure when).

v0.4
A group with a single object will select that object normally instead of grouped.

v0.3
Evans update.

v0.2
Added a mod option to show a marker when select & view is enabled.
