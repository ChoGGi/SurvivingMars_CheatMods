v0.9
Won't log spam when my lib mod isn't installed.

v0.8
Added code I use in ECM to allow pasting in more complicated code (no more "not understood").
Removes ` when using Tilde to open console.

v0.7
Added blacklist bypass used in ECM console.

v0.6
Disable mod when ECM is enabled.

v0.5
Evans update.

v0.4
Added "restart" func.

v0.3
Log showed back up even when turned off (when you open the console).

v0.2
Compatible with my lib mod.
