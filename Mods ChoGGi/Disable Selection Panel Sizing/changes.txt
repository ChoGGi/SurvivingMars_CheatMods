v0.5
Added height limit for infopanel.
Enabled scrollbar by default (flickering was fixed by devs sometime).

v0.4
Evans update.

v0.3
Added option to add scrollbars (buggy, has flickering).

v0.2
Added Mod Option to re-enable sizing.
